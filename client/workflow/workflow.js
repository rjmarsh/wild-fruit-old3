class Workflow {
	constructor(name, rootPerson, type, immediate, generations) {

		name.length;
		this._name = name;
		this._rootPerson = .rootPerson;
		this._type = type;
		this._immediate = immediate;
		this._generations = generations;

		this._lastModified = new Date();
	}
}

class Workflows {
	constructor() {};


}
